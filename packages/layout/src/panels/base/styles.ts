import { css } from 'lit';

export const styles = css`
    :host{
        position: relative;
        flex-grow: 1;
        min-width: 0;
        min-height: 0;
        background-color: var(--auto-bgcolor);
    }
`;
